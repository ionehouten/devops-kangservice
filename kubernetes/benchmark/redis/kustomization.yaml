apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: benchmark
labels:
  - pairs:
      environment: benchmark

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - job.yaml
  # if you want to run manually test via container
  # - deployment.yaml

